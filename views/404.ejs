<!DOCTYPE html>
<html lang="en">
  <head>
    <%- include('layouts/header', { title: '404 - Page Not Found' }) %>
    <link rel="stylesheet" href="/css/styles.css">
   
    <style>
      /* 404 Page Specific Styles */
      .error-container {
        max-width: 600px;
        margin: 2rem auto;
        background: white;
        padding: 2rem;
        border-radius: 10px;
        box-shadow: 0 4px 6px rgba(0, 0, 0, 0.1);
        text-align: center;
      }

      /* Error Icon */
      .error-icon {
        font-size: 5rem;
        color: #dc3545; /* Bootstrap danger color */
        margin-bottom: 1.5rem;
        animation: shake 0.5s ease-in-out infinite alternate;
      }

      @keyframes shake {
        from {
          transform: translateX(-10px);
        }
        to {
          transform: translateX(10px);
        }
      }

      /* Heading */
      .error-heading {
        font-size: 2.5rem;
        font-weight: bold;
        color: #333;
        margin-bottom: 1rem;
        transition: color 0.3s ease;
      }

      .error-heading:hover {
        color: #007bff;
      }

      /* Message */
      .error-message {
        font-size: 1.2rem;
        color: #6c757d;
        margin-bottom: 2rem;
      }

      /* Back Home Button */
      .error-btn-home {
        background: #007bff;
        color: #fff;
        border: none;
        padding: 12px 24px;
        font-size: 1rem;
        border-radius: 5px;
        cursor: pointer;
        transition: background 0.3s ease, transform 0.2s ease;
        display: inline-flex;
        align-items: center;
        gap: 0.5rem;
        text-decoration: none;
      }

      .error-btn-home:hover {
        background: #0056b3;
        transform: translateY(-2px);
      }

      .error-btn-home:active {
        transform: translateY(0);
      }

      .error-btn-home i {
        font-size: 1.2rem;
      }
    </style>
  </head>
  <body>
    <%- include('layouts/nav', { user: user }) %>

    <header class="masthead" style="background-image: url('/img/login-bg.jpg'); height: 10vh;">
      <div class="container position-relative px-4 px-lg-5">
        <div class="row gx-4 gx-lg-5 justify-content-center">
          <div class="col-md-10 col-lg-8 col-xl-7">
            <div class="site-heading">
              <h1>404 Error</h1>
              <span class="subheading">Page Not Found</span>
            </div>
          </div>
        </div>
      </div>
    </header>

    <div class="container px-5">
      <div class="error-container">
        <i class="fas fa-exclamation-triangle error-icon"></i>
        <h1 class="error-heading">Oops! Page Not Found</h1>
        <p class="error-message">
          The page you're looking for doesn't exist or has been moved. Let's get you back on track!
        </p>
        <a href="/" class="error-btn-home">
          <i class="fas fa-home"></i> Back to Home
        </a>
      </div>
    </div>

    <%- include('layouts/footer') %>
    <%- include('layouts/scripts') %>
  </body>
</html>